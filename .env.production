# Production frontend build (loaded by Vite for `vite build`, on top of .env).
# Empty API base => the frontend calls the SAME origin it was served from, so the
# single-process local deploy (backend serves the built SPA) needs no CORS and
# works on whatever host/port you open it with.
VITE_API_BASE=
VITE_APP_VERSION=local-1
